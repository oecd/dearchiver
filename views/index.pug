html
	head
		title= title
		link(href='/css/bootstrap.min.css' rel='stylesheet' type='text/css')
		script(src='//ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js' type='text/javascript')
		script.
			$(document).ready(function() {
				function hideMessage(selector) {
					$(selector).fadeTo(2000, 500).slideUp(500)
				}
				hideMessage('div.alert')
			})

	body
		div.container-fluid(style="padding-top: 2em")
			.row
				.col-lg-6.col-lg-offset-3
					.jumbotron
						h2 Dearchiver
						br
						p Welcome to the PAC Dearchiver. To get your files back, you need to enter a valid OECD/Kappa code. For example, #[mark 012003083].
						
						p Note that this #[mark only works for POD files] at the moment, but we're working on a version that will also dearchive prepress files.

						form.navbar-form.navbar-left(action='' method='post')
							.input-group.input-group-lg
								input.form-control(type='text' placeholder='Enter an OECD code ...' name='blobFile' id='blobFile')
								span.input-group-btn
									button.btn.btn-default(type='submit') Go!

						br
						br
						br
						br
						div#messageContainer(style="height: 2em;")
							if message == 'emptyCode'
								div.alert.alert-danger Please enter a code!
							if message == 'notFound'
								div.alert.alert-danger The code #{code} is not valid... Please, check!
							if message == 'success'
								div.alert.alert-success Downloading #{fileName}...
								script.
									var url = !{JSON.stringify(url)}
									window.location.href = url
